{
  "name": "iknowmio-landing",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate:sitemap": "node scripts/generate-sitemap.mjs",
    "generate": "npm run generate:sitemap && nuxt generate",
    "check:seo": "node scripts/check-seo.mjs",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "nuxt": "^3.14.0",
    "vue": "3.5.13"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "overrides": {
    "vue": "3.5.13"
  }
}
